# Copy the needed values into an untracked Gradle properties file or export the
# corresponding ROCKETFLOW_ANDROID_FIREBASE_* environment variables before
# assembleDebug / installDebug. Keep real values out of version control.

rocketflowFirebaseApplicationId=
rocketflowFirebaseApiKey=
rocketflowFirebaseProjectId=
rocketflowFirebaseGcmSenderId=
